CREATE TABLE [dbo].[VerificationType] (
    [VerificationTypeId]   INT           NOT NULL,
    [VerificationTypeName] NVARCHAR (50) NULL,
    CONSTRAINT [PK_VerificationType] PRIMARY KEY CLUSTERED ([VerificationTypeId] ASC)
);

