CREATE TABLE [dbo].[GuaranteeDistinations] (
    [ID]                  INT           NOT NULL,
    [GuarnteeDistination] NVARCHAR (50) NULL,
    CONSTRAINT [PK_GuaranteeDistinations] PRIMARY KEY CLUSTERED ([ID] ASC)
);

